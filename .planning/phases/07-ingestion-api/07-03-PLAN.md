---
phase: 07-ingestion-api
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - tests/test_ingestion_e2e.py
autonomous: true
requirements: [API-01, API-02, PIPE-01]

must_haves:
  truths:
    - "An end-to-end test drives a real PDF through POST /documents -> Celery task -> Neo4j and asserts a Candidate landed in the graph (ROADMAP criterion #4)"
    - "The test polls GET /documents/{id} until processing_status reaches the MINIMAL D-01 set value 'written' (NOT per-stage statuses — deviation per D-02), with a timeout"
    - "After 'written', scripts/queries.py::find_candidates_by_skill finds the new candidate by document_id (criterion #4)"
    - "A failure-path assertion: a corrupt/non-resume input results in processing_status='failed' with a non-null failed_stage (D-06), and a repeat POST of an already-written doc returns task_id=null without re-processing (D-05)"
    - "The test SKIPS cleanly when infra (Neo4j / Celery worker / OPENROUTER_API_KEY) is unavailable — never a hard failure in a bare CI (mirrors test_parser_integration.py / test_extractor_integration.py)"
  artifacts:
    - path: "tests/test_ingestion_e2e.py"
      provides: "End-to-end ingestion smoke test"
      contains: "find_candidates_by_skill"
  key_links:
    - from: "tests/test_ingestion_e2e.py"
      to: "api/routers/documents.py POST/GET"
      via: "TestClient or httpx against the FastAPI app"
      pattern: "/documents"
    - from: "tests/test_ingestion_e2e.py"
      to: "scripts/queries.py find_candidates_by_skill"
      via: "post-written graph assertion"
      pattern: "find_candidates_by_skill"
---

<objective>
Prove the whole ingestion pipeline works through the public surface: a real PDF posted to `POST /documents` is processed by the Celery worker and lands as a Candidate in Neo4j, discoverable via `scripts/queries.py`. This is the phase's exit criterion (#4) and the smoke test for PIPE-01 + API-01 + API-02. It also asserts the D-05 dedup and D-06 failure-diagnostics behaviors.

Purpose: ROADMAP Phase 7 success criteria #4 and #5.
Output: `tests/test_ingestion_e2e.py` — skips cleanly without infra; green when Neo4j + a Celery worker + OPENROUTER_API_KEY are available.
</objective>

<execution_context>
@C:/dev/HUMAN.AI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/HUMAN.AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ingestion-api/07-CONTEXT.md
@CLAUDE.md
@.planning/phases/07-ingestion-api/07-01-PLAN.md
@.planning/phases/07-ingestion-api/07-02-PLAN.md

<interfaces>
<!-- Test-only contracts. Use directly. -->

From scripts/queries.py:
```python
async def find_candidates_by_skill(driver: AsyncDriver, skill_name: str) -> list[dict]  # [{"id","full_name"}]
async def find_candidates_by_company(driver: AsyncDriver, company_name: str) -> list[dict]
```

From tests/conftest.py (reuse these fixtures):
```python
@pytest.fixture(scope="session") def settings() -> Settings
@pytest_asyncio.fixture(scope="session") async def neo4j_driver -> AsyncDriver
@pytest_asyncio.fixture(loop_scope="session", scope="session") async def graph_db -> GraphDB  # connect_with_retry(1,[0])
```

Skip pattern (mirror tests/test_parser_integration.py line 21-23 and test_extractor_integration.py):
```python
pytestmark = pytest.mark.asyncio(loop_scope="session")
if not graph_db.is_connected: pytest.skip("Neo4j unavailable")
if not settings.openrouter_api_key: pytest.skip("OPENROUTER_API_KEY not set")
```

API surface (07-02): POST /documents (multipart field "file") -> 202 {document_id, task_id}; GET /documents/{id} -> {document_id, processing_status, error, failed_stage}.
Pipeline statuses (07-01): "queued" | "processing" | "written" | "failed".
Sample resumes: rnd/data/resume/*.pdf (used by existing integration tests).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end happy-path + dedup smoke test (criteria #4, #5 / D-05)</name>
  <read_first>
    - tests/test_ingestion_e2e.py (creating)
    - tests/test_parser_integration.py (skip-when-Neo4j-down pattern; rnd/data/resume glob)
    - tests/test_extractor_integration.py (skip-when-no-OPENROUTER_API_KEY pattern)
    - tests/conftest.py (settings/neo4j_driver/graph_db fixtures + loop_scope)
    - scripts/queries.py (find_candidates_by_skill / find_candidates_by_company signatures)
    - api/routers/documents.py (POST/GET shapes — 07-02)
    - core/pipeline/tasks.py (process_document — 07-01; the test may run it eagerly, see action)
  </read_first>
  <action>
    Create tests/test_ingestion_e2e.py with `pytestmark = pytest.mark.asyncio(loop_scope="session")`.

    Worker strategy (no live broker needed — keep CI-runnable): configure Celery eager mode so `.delay()` executes inline. At test setup do `from core.pipeline.celery_app import celery_app; celery_app.conf.task_always_eager = True; celery_app.conf.task_eager_propagates = True`. This runs `process_document` synchronously inside the test process against the SAME Neo4j the fixtures use — proving the orchestration end to end without a separate worker process. (Document this clearly: the test exercises the real task body, not a mock.)

    Test `test_ingestion_happy_path(graph_db, neo4j_driver, settings)`:
    1. Skip if `not graph_db.is_connected` ("Neo4j unavailable"); skip if `not settings.openrouter_api_key` ("OPENROUTER_API_KEY not set" — extract step is a live paid call).
    2. Build the FastAPI app TestClient; wire `app.state.db = graph_db`, `app.state.settings = settings` (lifespan won't run under TestClient unless used as context manager — set state explicitly OR use `with TestClient(app) as client:`).
    3. Pick `pdf = next(Path("rnd/data/resume").glob("*.pdf"))`; `resp = client.post("/documents", files={"file": (pdf.name, pdf.read_bytes(), "application/pdf")})`. Assert 202; capture `document_id`. With eager mode the task already ran during `.delay()`.
    4. `g = client.get(f"/documents/{document_id}")`; assert 200 and `g.json()["processing_status"] == "written"` (D-01 minimal set — NOT a per-stage value; deviation D-02). If you cannot rely on eager completion timing, poll GET up to ~60s with short sleeps until status in {"written","failed"}; assert "written".
    5. Graph assertion (criterion #4): `rows = await find_candidates_by_skill(neo4j_driver, <a skill known to appear>, )` — since skills vary by resume, instead assert by id: `MATCH (c:Candidate {id:$document_id}) RETURN c` returns a node AND `find_candidates_by_company`/`find_candidates_by_skill` returns at least one row whose id == document_id for a skill read back from that candidate's graph. Simplest robust check: query the candidate's skills from Neo4j, pick one, then assert `find_candidates_by_skill(driver, that_skill)` contains document_id.
    6. Dedup (criterion #5 / D-05): POST the same bytes again; assert response status 200 and `task_id is None` (already written -> no re-processing).

    Cleanup: best-effort `DETACH DELETE` of the candidate/document created (so reruns stay clean), guarded in a finally.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ingestion_e2e.py::test_ingestion_happy_path -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "find_candidates_by_skill" tests/test_ingestion_e2e.py` matches
    - `grep -n "task_always_eager" tests/test_ingestion_e2e.py` matches
    - `grep -n '"written"' tests/test_ingestion_e2e.py` matches (asserts minimal D-01 status, not per-stage)
    - `grep -n "task_id" tests/test_ingestion_e2e.py` matches (D-05 dedup assertion)
    - `grep -n "pytest.skip" tests/test_ingestion_e2e.py` matches (clean skip without infra)
    - `python -m pytest tests/test_ingestion_e2e.py::test_ingestion_happy_path -q` exits 0 (passes OR skips cleanly — never errors)
  </acceptance_criteria>
  <done>End-to-end test posts a real PDF, drives the real task eagerly, polls GET to "written", and confirms the candidate via scripts/queries.py; re-POST returns task_id=null. Skips cleanly without Neo4j/API key.</done>
</task>

<task type="auto">
  <name>Task 2: Failure-path test — failed status + failed_stage (criterion #5 / D-06)</name>
  <read_first>
    - tests/test_ingestion_e2e.py (extend — created in Task 1)
    - core/pipeline/tasks.py (set_failed per-stage — 07-01)
    - api/routers/documents.py (GET returns error + failed_stage — 07-02)
  </read_first>
  <action>
    Add `test_ingestion_failure_path(graph_db, settings)` to tests/test_ingestion_e2e.py. Skip if `not graph_db.is_connected`. With Celery still in eager+propagate mode, POST an input that makes the pipeline fail BEFORE the paid LLM call — use a minimal but non-extractable PDF (e.g. a tiny valid-but-empty PDF from the corpus that parses to empty text, or craft bytes with a `.pdf` name that pypdf rejects). Two acceptable approaches — pick one and document it:
      (a) Monkeypatch `core.pipeline.tasks.Extractor` so `extract` raises RuntimeError -> drives failed_stage="extract" deterministically WITHOUT needing OPENROUTER_API_KEY (preferred: no paid call, no skip on missing key).
      (b) Feed a corrupt PDF to force failed_stage="parse".
    Because eager mode propagates, `.delay()` will re-raise the task exception; wrap the POST in the expectation that either (i) the API still returns 202 and the node is marked failed by set_failed before the raise, or (ii) the eager raise surfaces — in eager+propagate the task body's set_failed runs first, so assert via GET afterward. Then:
    - `g = client.get(f"/documents/{document_id}")`; assert `g.json()["processing_status"] == "failed"` and `g.json()["failed_stage"] in {"parse","extract","write"}` and `error` is a non-empty string (D-06).
    - D-05 failed-reprocess branch: POST the same doc again; assert 202 and `task_id is not None` (failed -> re-enqueued, per D-05).
    Cleanup the created node in a finally.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ingestion_e2e.py::test_ingestion_failure_path -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "failed_stage" tests/test_ingestion_e2e.py` matches
    - `grep -n '"failed"' tests/test_ingestion_e2e.py` matches
    - `grep -n "task_id is not None\|task_id\"\] is not None\|assertIsNotNone" tests/test_ingestion_e2e.py` matches (failed re-enqueue, D-05)
    - `python -m pytest tests/test_ingestion_e2e.py::test_ingestion_failure_path -q` exits 0 (passes OR skips cleanly)
  </acceptance_criteria>
  <done>Failure-path test asserts processing_status='failed' + non-null failed_stage + error (D-06), and that a re-POST of a failed doc re-enqueues (D-05). Skips cleanly without Neo4j.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> FastAPI app | the test injects fixtures into app.state (trusted test context) |
| eager task -> live Neo4j | the test writes/reads real graph data and cleans up |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | test pollutes the shared graph | mitigate | Test DETACH DELETEs the candidate/document it creates in a finally block; ids are deterministic so reruns are idempotent. |
| T-07-12 | Info disclosure | OPENROUTER_API_KEY in test logs | accept | Key is read from Settings (never logged by extractor T-05-05); the test only checks for its presence to decide skip, never prints it. |
| T-07-13 | Denial of Service | live LLM cost in CI | mitigate | Happy-path skips without OPENROUTER_API_KEY; failure-path uses a monkeypatched Extractor (no paid call) so it runs in bare CI. |
</threat_model>

<verification>
- `python -m pytest tests/test_ingestion_e2e.py -q` exits 0 (passes with full infra, skips cleanly without)
- With Neo4j up + OPENROUTER_API_KEY set + a worker (or eager mode): a real PDF -> Candidate found by find_candidates_by_skill (criterion #4)
- ruff check tests/test_ingestion_e2e.py passes
</verification>

<success_criteria>
- Criterion #4: POST real PDF -> poll GET to "written" -> find_candidates_by_skill finds the candidate
- Criterion #5: failure -> status "failed" + failed_stage + error (D-06); re-POST of written -> no dup (D-05), re-POST of failed -> re-enqueue (D-05)
- Test skips cleanly in bare CI (no Neo4j / no API key)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingestion-api/07-03-SUMMARY.md`
</output>
