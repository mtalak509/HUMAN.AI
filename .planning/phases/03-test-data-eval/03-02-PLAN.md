---
id: 03-02
phase: 3
wave: 1
depends_on: []
files_modified:
  - scripts/queries.py
requirements_addressed:
  - SEED-02
autonomous: true
---

# Plan 03-02: Примеры Cypher-запросов

## Objective

Создать `scripts/queries.py` — библиотеку из трёх async-функций поиска кандидатов. Функции импортируются в тесты. Каждая принимает `AsyncDriver`, выполняет параметризованный Cypher и возвращает `list[dict]`.

## Tasks

<task id="03-02-T1">
<title>Create scripts/queries.py</title>

<read_first>
- `core_architecture.md` §4.3 — связи между узлами (граф-паттерн для MATCH-запросов)
- `core/schemas/models.py` — типы узлов и их ключевые поля (проверить имена свойств)
- `scripts/seed.py` (если уже написан) — конкретные данные кандидата для проверки запросов
</read_first>

<action>
Создать `scripts/queries.py` — три async-функции, каждая с Cypher-запросом в docstring.

### Импорты

```python
from neo4j import AsyncDriver
```

### Функция 1: find_candidates_by_skill

```python
async def find_candidates_by_skill(driver: AsyncDriver, skill_name: str) -> list[dict]:
    """
    Find candidates who have a specific skill.

    Cypher:
        MATCH (c:Candidate)-[:HAS_SKILL]->(s:Skill)
        WHERE toLower(s.name) = toLower($skill_name)
        RETURN c.id AS id, c.full_name AS full_name
    """
    async with driver.session() as session:
        result = await session.run(
            "MATCH (c:Candidate)-[:HAS_SKILL]->(s:Skill) "
            "WHERE toLower(s.name) = toLower($skill_name) "
            "RETURN c.id AS id, c.full_name AS full_name",
            skill_name=skill_name,
        )
        return [dict(record) async for record in result]
```

### Функция 2: find_candidates_by_company

```python
async def find_candidates_by_company(driver: AsyncDriver, company_name: str) -> list[dict]:
    """
    Find candidates with experience at a specific company.

    Cypher:
        MATCH (c:Candidate)-[:HAS_EXPERIENCE]->(e:Experience)-[:AT_COMPANY]->(co:Company)
        WHERE toLower(co.name) = toLower($company_name)
        RETURN DISTINCT c.id AS id, c.full_name AS full_name
    """
    async with driver.session() as session:
        result = await session.run(
            "MATCH (c:Candidate)-[:HAS_EXPERIENCE]->(e:Experience)-[:AT_COMPANY]->(co:Company) "
            "WHERE toLower(co.name) = toLower($company_name) "
            "RETURN DISTINCT c.id AS id, c.full_name AS full_name",
            company_name=company_name,
        )
        return [dict(record) async for record in result]
```

### Функция 3: find_candidates_by_status

```python
async def find_candidates_by_status(
    driver: AsyncDriver, vacancy_id: str, status: str
) -> list[dict]:
    """
    Find candidates with a specific status in a vacancy pipeline.

    Cypher:
        MATCH (c:Candidate)-[:REACHED_STATUS]->(st:Status)-[:IN_VACANCY]->(v:Vacancy)
        WHERE v.id = $vacancy_id AND st.name = $status
        RETURN c.id AS id, c.full_name AS full_name
    """
    async with driver.session() as session:
        result = await session.run(
            "MATCH (c:Candidate)-[:REACHED_STATUS]->(st:Status)-[:IN_VACANCY]->(v:Vacancy) "
            "WHERE v.id = $vacancy_id AND st.name = $status "
            "RETURN c.id AS id, c.full_name AS full_name",
            vacancy_id=vacancy_id,
            status=status,
        )
        return [dict(record) async for record in result]
```

No `if __name__ == "__main__"` block required. This is a library module.
</action>

<acceptance_criteria>
- `scripts/queries.py` exists: `test -f scripts/queries.py`
- All 3 functions defined: `grep -c "^async def find_candidates_by" scripts/queries.py` → `3`
- find_candidates_by_skill present: `grep -c "def find_candidates_by_skill" scripts/queries.py` → `1`
- find_candidates_by_company present: `grep -c "def find_candidates_by_company" scripts/queries.py` → `1`
- find_candidates_by_status present: `grep -c "def find_candidates_by_status" scripts/queries.py` → `1`
- AsyncDriver type hint present: `grep -c "AsyncDriver" scripts/queries.py` → at least `3`
- Return type list[dict] present: `grep -c "list\[dict\]" scripts/queries.py` → at least `3`
- Cypher uses HAS_SKILL rel: `grep -c "HAS_SKILL" scripts/queries.py` → `1`
- Cypher uses AT_COMPANY rel: `grep -c "AT_COMPANY" scripts/queries.py` → `1`
- Cypher uses REACHED_STATUS rel: `grep -c "REACHED_STATUS" scripts/queries.py` → `1`
- Cypher uses IN_VACANCY rel: `grep -c "IN_VACANCY" scripts/queries.py` → `1`
- Parameters use $skill_name: `grep -c "\$skill_name" scripts/queries.py` → `1`
- Parameters use $vacancy_id: `grep -c "\$vacancy_id" scripts/queries.py` → `1`
- Python syntax valid: `python -c "import ast; ast.parse(open('scripts/queries.py').read())"` exits 0
- Module importable: `python -c "from scripts.queries import find_candidates_by_skill, find_candidates_by_company, find_candidates_by_status"` exits 0
</acceptance_criteria>
</task>

## Verification

`python -c "from scripts.queries import find_candidates_by_skill"` exits 0.

On live Neo4j with seed data:
- `find_candidates_by_skill(driver, "Python")` → `[{"id": "c-001", "full_name": "Алексей Соколов"}]`
- `find_candidates_by_company(driver, "TechFlow Analytics")` → `[{"id": "c-001", "full_name": "Алексей Соколов"}]`
- `find_candidates_by_status(driver, "v-001", "in_progress")` → `[{"id": "c-001", "full_name": "Алексей Соколов"}]`

## must_haves

<must_haves>
<truths>
- All 3 functions accept AsyncDriver as first argument (not GraphDB wrapper) — conftest fixture passes driver directly
- Cypher relationship names match core_architecture.md §4.3 exactly: HAS_SKILL, HAS_EXPERIENCE, AT_COMPANY, REACHED_STATUS, IN_VACANCY
- Case-insensitive matching via toLower() for skill and company name queries
- DISTINCT in find_candidates_by_company — candidate with multiple experiences at same company appears once
- Cypher queries documented in docstrings
- No GraphDB import — queries.py is a pure function library, no infrastructure coupling
</truths>
</must_haves>
