---
id: 03-03
phase: 3
wave: 2
depends_on:
  - 03-02
files_modified:
  - tests/conftest.py
  - tests/test_infra.py
requirements_addressed:
  - TEST-01
  - TEST-02
autonomous: true
---

# Plan 03-03: Eval-харнес

## Objective

Создать `tests/conftest.py` с тремя session-scoped pytest-фикстурами (`settings`, `neo4j_driver`, `qdrant_client`) и `tests/test_infra.py` с тремя smoke-тестами (Neo4j `RETURN 1`, Qdrant `/health`, Redis `ping`). `pytest tests/test_infra.py` должен проходить зелёным при поднятом стеке.

## Tasks

<task id="03-03-T1">
<title>Create tests/conftest.py</title>

<read_first>
- `core/config.py` — Settings class, атрибуты: neo4j_uri, neo4j_user, neo4j_password, qdrant_url, redis_url
- `core/database/graph.py` — GraphDB import path (canonical: `from core.database.graph import GraphDB`)
- `pyproject.toml` — зависимости: `neo4j>=5.20`, `qdrant-client>=1.9`, `celery[redis]>=5.4`
- `pyproject.toml` — pytest config: `asyncio_mode = "auto"` (async fixtures работают без @pytest_asyncio.fixture в auto-mode)
- `tests/test_health.py` — пример существующих тестов (структура, imports)
</read_first>

<action>
Создать `tests/conftest.py` с тремя session-scoped фикстурами.

### Импорты

```python
from collections.abc import AsyncGenerator, Generator
from typing import TYPE_CHECKING

import pytest
import pytest_asyncio
from neo4j import AsyncDriver, AsyncGraphDatabase
from qdrant_client import QdrantClient

from core.config import Settings, get_settings
```

### Фикстура 1: settings

```python
@pytest.fixture(scope="session")
def settings() -> Settings:
    return get_settings()
```

### Фикстура 2: neo4j_driver

Создаёт raw AsyncDriver (не GraphDB) — именно этот тип принимают функции queries.py.

```python
@pytest_asyncio.fixture(scope="session")
async def neo4j_driver(settings: Settings) -> AsyncGenerator[AsyncDriver, None]:
    driver = AsyncGraphDatabase.driver(
        settings.neo4j_uri,
        auth=(settings.neo4j_user, settings.neo4j_password),
    )
    yield driver
    await driver.close()
```

### Фикстура 3: qdrant_client

```python
@pytest.fixture(scope="session")
def qdrant_client(settings: Settings) -> Generator[QdrantClient, None, None]:
    client = QdrantClient(url=str(settings.qdrant_url))
    yield client
    client.close()
```

Нет `redis_client` в conftest.py — Redis проверяется inline в test_infra.py через `redis.asyncio`.
</action>

<acceptance_criteria>
- `tests/conftest.py` exists: `test -f tests/conftest.py`
- settings fixture present: `grep -c "def settings" tests/conftest.py` → `1`
- neo4j_driver fixture present: `grep -c "def neo4j_driver" tests/conftest.py` → `1`
- qdrant_client fixture present: `grep -c "def qdrant_client" tests/conftest.py` → `1`
- All fixtures are session-scoped: `grep -c 'scope="session"' tests/conftest.py` → `3`
- AsyncDriver yield: `grep -c "AsyncDriver" tests/conftest.py` → at least `2`
- QdrantClient present: `grep -c "QdrantClient" tests/conftest.py` → at least `2`
- get_settings used: `grep -c "get_settings()" tests/conftest.py` → `1`
- Python syntax valid: `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` exits 0
</acceptance_criteria>
</task>

<task id="03-03-T2">
<title>Create tests/test_infra.py</title>

<read_first>
- `tests/conftest.py` (just written) — фикстуры: settings, neo4j_driver, qdrant_client — их сигнатуры и возвращаемые типы
- `core/config.py` — Settings.redis_url тип (str или AnyUrl)
- `pyproject.toml` — `asyncio_mode = "auto"` (async def test_ работают без декоратора)
</read_first>

<action>
Создать `tests/test_infra.py` с тремя smoke-тестами. Каждый тест проверяет один инфраструктурный сервис.

### Импорты

```python
import redis.asyncio as aioredis
from neo4j import AsyncDriver
from qdrant_client import QdrantClient

from core.config import Settings
```

### Тест 1: Neo4j ping (RETURN 1)

```python
async def test_neo4j_ping(neo4j_driver: AsyncDriver) -> None:
    async with neo4j_driver.session() as session:
        result = await session.run("RETURN 1 AS n")
        record = await result.single()
    assert record is not None
    assert record["n"] == 1
```

### Тест 2: Qdrant health

```python
def test_qdrant_health(qdrant_client: QdrantClient) -> None:
    collections = qdrant_client.get_collections()
    assert collections is not None
```

### Тест 3: Redis ping

```python
async def test_redis_ping(settings: Settings) -> None:
    r = aioredis.from_url(str(settings.redis_url))
    try:
        pong = await r.ping()
    finally:
        await r.aclose()
    assert pong is True
```
</action>

<acceptance_criteria>
- `tests/test_infra.py` exists: `test -f tests/test_infra.py`
- 3 test functions present: `grep -c "^async def test_\|^def test_" tests/test_infra.py` → `3`
- Neo4j test present: `grep -c "def test_neo4j_ping" tests/test_infra.py` → `1`
- Qdrant test present: `grep -c "def test_qdrant_health" tests/test_infra.py` → `1`
- Redis test present: `grep -c "def test_redis_ping" tests/test_infra.py` → `1`
- RETURN 1 query in Neo4j test: `grep -c "RETURN 1" tests/test_infra.py` → `1`
- get_collections in Qdrant test: `grep -c "get_collections" tests/test_infra.py` → `1`
- redis.asyncio ping in Redis test: `grep -c "\.ping()" tests/test_infra.py` → `1`
- aioredis.from_url present: `grep -c "from_url" tests/test_infra.py` → `1`
- Python syntax valid: `python -c "import ast; ast.parse(open('tests/test_infra.py').read())"` exits 0
- Tests collected without error (imports resolve): `pytest tests/test_infra.py --collect-only` exits 0
</acceptance_criteria>
</task>

## Verification

`pytest tests/test_infra.py` passes (3 tests green) when `docker compose up -d neo4j qdrant redis` is running.

## must_haves

<must_haves>
<truths>
- settings, neo4j_driver, qdrant_client fixtures are session-scoped — initialized once per test run, not per test
- neo4j_driver fixture yields AsyncDriver (raw neo4j driver), not GraphDB wrapper — matches queries.py function signature
- asyncio_mode = "auto" is already in pyproject.toml — no @pytest.mark.asyncio needed on individual tests or fixtures
- test_infra.py has exactly 3 tests: one per infra service (Neo4j, Qdrant, Redis)
- Redis test uses redis.asyncio (async) to match the async test context — synchronous redis would need a sync test function
</truths>
</must_haves>
