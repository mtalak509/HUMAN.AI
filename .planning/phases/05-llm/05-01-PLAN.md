---
phase: 05-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/extractor/__init__.py
  - core/extractor/schema.py
  - core/config.py
  - tests/test_extractor_schema.py
autonomous: true
requirements: [EXTR-03]
must_haves:
  truths:
    - "ExtractedCandidate валидирует выход LLM по полям онтологии (full_name, contacts, experiences, education, skills)"
    - "Каждый Experience несёт is_current, вычисляемый из to_date is None"
    - "ExtractedCandidate несёт top-level провенанс: document_id и model_version"
    - "Все 5 эталонных rnd/data/results/*.parsed.json валидируются rnd-частью схемы без ошибок"
    - "Settings содержит конфиг-кнобы экстрактора (extractor_model, openrouter_base_url, extractor_timeout, extractor_temperature)"
  artifacts:
    - path: "core/extractor/schema.py"
      provides: "ExtractedCandidate + Contact/Experience/Education Pydantic v2 модели"
      contains: "class ExtractedCandidate(BaseModel)"
      min_lines: 40
    - path: "core/extractor/__init__.py"
      provides: "public re-export ExtractedCandidate (и позже Extractor)"
      contains: "from core.extractor.schema import"
    - path: "core/config.py"
      provides: "extractor config fields в Settings"
      contains: "extractor_model"
    - path: "tests/test_extractor_schema.py"
      provides: "equivalence-валидация 5 эталонных parsed.json + is_current"
      contains: "parsed.json"
  key_links:
    - from: "core/extractor/schema.py::Experience"
      to: "to_date is None"
      via: "computed is_current (model_validator или computed_field)"
      pattern: "is_current"
    - from: "core/extractor/__init__.py"
      to: "core/extractor/schema.py"
      via: "re-export"
      pattern: "from core\\.extractor\\.schema import"
---

<objective>
Создать `core/extractor/schema.py` — Pydantic v2 модель `ExtractedCandidate` (перенос rnd-схемы `Resume` дословно по полям + дельта `is_current` на Experience + top-level провенанс document_id/model_version), публичный re-export в `core/extractor/__init__.py`, и конфиг-кнобы экстрактора в `Settings`.

Purpose: Зафиксировать контракт выхода LLM ДО реализации Extractor (план 05-02 возвращает `ExtractedCandidate`). Interface-first: контракт идёт первым, реализация второй.
Output: `core/extractor/schema.py`, `core/extractor/__init__.py`, дополнения в `core/config.py`, `tests/test_extractor_schema.py`.

Покрывает: EXTR-03 (schema охватывает full_name, contacts, experiences с датами/компанией/ролью/навыками, education, skills).
Реализует: D-02 (top-level провенанс), D-04 (rnd-схема дословно), D-05 (is_current per-experience), D-06 (без переименования под онтологию), Claude's Discretion (config-кнобы).
</objective>

<execution_context>
@C:/dev/HUMAN.AI/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/HUMAN.AI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-llm/05-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- rnd Resume-схема (D-04: переносится дословно по полям). Из rnd/src/json_schema.py: -->
```python
class Contact(BaseModel):
    type: Literal["email", "phone", "telegram", "linkedin", "other"]
    value: str

class Experience(BaseModel):
    from_date: str = Field(description="YYYY-MM или YYYY")
    to_date: Optional[str] = Field(default=None, description="None если по настоящее время")
    company: str
    role: str
    description: Optional[str] = None
    skills_mentioned: list[str] = Field(default_factory=list)

class Education(BaseModel):
    institution: str
    degree: Optional[str] = None
    field: Optional[str] = None
    from_date: Optional[str] = None
    to_date: Optional[str] = None

class Resume(BaseModel):
    full_name: str
    contacts: list[Contact] = Field(default_factory=list)
    experiences: list[Experience] = Field(default_factory=list)
    education: list[Education] = Field(default_factory=list)
    skills: list[str] = Field(default_factory=list)
```

<!-- Settings config-add pattern (из core/config.py, поле storage_root): -->
```python
storage_root: Path = Field(
    default=Path("storage"),
    description="...",
)
# openrouter_api_key: str | None уже присутствует — НЕ дублировать.
```

<!-- Re-export shim pattern (из core/parser/__init__.py): -->
```python
from core.parser.pdf import ParseResult, PdfParser
__all__ = ["PdfParser", "ParseResult"]
```

<!-- Эталонная структура parsed.json (rnd/data/results/Markova.parsed.json) —
     top-level: full_name (str), contacts[{type,value}], experiences[{from_date,to_date,company,role,description,skills_mentioned}],
     education[{institution,degree,field,from_date,to_date}], skills[str].
     ВАЖНО: эти файлы НЕ содержат is_current/document_id/model_version — они только rnd-часть. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExtractedCandidate Pydantic-схема + re-export</name>
  <read_first>
    - rnd/src/json_schema.py (rnd Resume/Contact/Experience/Education — переносится дословно по полям)
    - core/parser/__init__.py (re-export shim pattern)
    - core/schemas/models.py (awareness онтологии: Experience.is_current — имя поля совпадает)
    - rnd/data/results/Markova.parsed.json (форма эталонного выхода для теста эквивалентности)
  </read_first>
  <behavior>
    - Test: Contact(type="email", value="a@b.c") валиден; type="garbage" → ValidationError.
    - Test: Experience(from_date="2021-08", to_date=None, company="X", role="Y").is_current is True.
    - Test: Experience(..., to_date="2024-11").is_current is False.
    - Test: ExtractedCandidate(full_name="N", document_id="abc", model_version="qwen/x") валиден; пустые списки по умолчанию.
    - Test: каждый rnd/data/results/*.parsed.json валидируется через ExtractedCandidate.model_validate(<json> + {"document_id":"...","model_version":"..."}) без ошибок для всех 5 файлов.
  </behavior>
  <action>
    Создай `core/extractor/schema.py` (Pydantic v2). Перенеси rnd-классы ДОСЛОВНО по полям (D-04):

    - `Contact(BaseModel)`: `type: Literal["email","phone","telegram","linkedin","other"]`, `value: str`.
    - `Education(BaseModel)`: `institution: str`, `degree: str | None = None`, `field: str | None = None`, `from_date: str | None = None`, `to_date: str | None = None`.
    - `Experience(BaseModel)`: `from_date: str = Field(description="YYYY-MM или YYYY")`, `to_date: str | None = Field(default=None, ...)`, `company: str`, `role: str`, `description: str | None = None`, `skills_mentioned: list[str] = Field(default_factory=list)`.
      Добавь дельту D-05: `is_current` как `@computed_field @property def is_current(self) -> bool: return self.to_date is None`. (computed_field, чтобы поле сериализовалось в model_dump/JSON и НЕ требовалось от LLM на входе.)
    - `ExtractedCandidate(BaseModel)`: rnd-поля дословно — `full_name: str`, `contacts: list[Contact] = Field(default_factory=list)`, `experiences: list[Experience] = Field(default_factory=list)`, `education: list[Education] = Field(default_factory=list)`, `skills: list[str] = Field(default_factory=list)`. Плюс top-level провенанс (D-02): `document_id: str`, `model_version: str`.

    НЕ переименовывай поля под имена онтологии (company→Company.name и т.п.) — это маппинг Фазы 6 (D-06). НЕ делай frozen dataclass — это `pydantic.BaseModel` (валидированный выход LLM).

    Импорты: `from typing import Literal`, `from pydantic import BaseModel, Field, computed_field`. Используй PEP 604 (`str | None`), не `Optional`.

    Обнови `core/extractor/__init__.py`: `from core.extractor.schema import Contact, Education, Experience, ExtractedCandidate` и `__all__ = ["ExtractedCandidate", "Contact", "Experience", "Education"]`. (План 05-02 добавит сюда Extractor.)

    Напиши `tests/test_extractor_schema.py` по списку behavior. Equivalence-тест: `for p in Path("rnd/data/results").glob("*.parsed.json"): data = json.loads(p.read_text(encoding="utf-8")); ExtractedCandidate.model_validate({**data, "document_id": "test", "model_version": "test"})` — без исключений для всех 5. Тесты НЕ требуют инфры/сети.
  </action>
  <verify>
    <automated>pytest tests/test_extractor_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `core/extractor/schema.py` содержит `class ExtractedCandidate(BaseModel)` (grep).
    - `core/extractor/schema.py` содержит `is_current` и `computed_field` (grep).
    - `grep -c 'Optional' core/extractor/schema.py` = 0 (PEP 604, без Optional).
    - `core/extractor/__init__.py` содержит `from core.extractor.schema import` (grep).
    - `pytest tests/test_extractor_schema.py` выходит с кодом 0.
    - Equivalence-тест валидирует все 5 файлов rnd/data/results/*.parsed.json (тест присутствует и зелёный).
  </acceptance_criteria>
  <done>
    ExtractedCandidate валидирует rnd-форму + провенанс; Experience.is_current вычисляется из to_date; все 5 эталонных parsed.json валидируются; re-export работает.
  </done>
</task>

<task type="auto">
  <name>Task 2: Конфиг-кнобы экстрактора в Settings</name>
  <read_first>
    - core/config.py (Settings, паттерн поля storage_root; openrouter_api_key уже есть — НЕ дублировать)
    - rnd/src/openrouter_client.py (дефолты: model="qwen/qwen3.6-plus", base_url="https://openrouter.ai/api/v1", timeout=60.0, temperature=0)
    - CLAUDE.md (Settings cached / cache_clear в тестах)
  </read_first>
  <action>
    Добавь в `core/config.py::Settings` (паттерн поля `storage_root`), рядом с `openrouter_api_key`:

    - `extractor_model: str = Field(default="qwen/qwen3.6-plus", description="OpenRouter model id for the LLM extractor; env EXTRACTOR_MODEL")`
    - `openrouter_base_url: str = Field(default="https://openrouter.ai/api/v1", description="OpenRouter API base URL; env OPENROUTER_BASE_URL")`
    - `extractor_timeout: float = Field(default=60.0, description="LLM call timeout in seconds; env EXTRACTOR_TIMEOUT")`
    - `extractor_temperature: float = Field(default=0.0, description="LLM sampling temperature; env EXTRACTOR_TEMPERATURE")`

    Дефолты = значения smoke-test (rnd/src/openrouter_client.py). `api_key` НЕ добавляй — читается из существующего `openrouter_api_key`. env-имена выводятся из имён полей автоматически (case_insensitive).

    Добавь в `tests/test_extractor_schema.py` (или отдельный блок) тест: `get_settings.cache_clear(); s = get_settings(); assert s.extractor_model == "qwen/qwen3.6-plus"; assert s.openrouter_base_url == "https://openrouter.ai/api/v1"; assert s.extractor_timeout == 60.0; assert s.extractor_temperature == 0.0`. Не забудь `get_settings.cache_clear()` (CLAUDE.md).
  </action>
  <verify>
    <automated>pytest tests/test_extractor_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'extractor_model' core/config.py` >= 1.
    - `core/config.py` содержит `openrouter_base_url`, `extractor_timeout`, `extractor_temperature` (grep).
    - `core/config.py` НЕ содержит второго определения `openrouter_api_key` (grep -c = 1).
    - Тест на дефолты Settings присутствует и зелёный (`pytest tests/test_extractor_schema.py` код 0).
  </acceptance_criteria>
  <done>
    Settings несёт extractor_model/openrouter_base_url/extractor_timeout/extractor_temperature с дефолтами smoke-test; api_key не дублирован; тест дефолтов зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → ExtractedCandidate | Выход LLM недоверенный; Pydantic-валидация — граница доверия (план 05-02 потребляет схему). |
| .env → Settings | Секреты (openrouter_api_key) попадают в процесс только через pydantic-settings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | LLM-выход, валидируемый ExtractedCandidate | mitigate | Строгая Pydantic v2 схема: Literal на Contact.type, обязательные company/role; невалидный JSON отклоняется (использование схемы — в 05-02). |
| T-05-02 | Information Disclosure | openrouter_api_key в Settings | mitigate | Этот план НЕ логирует и НЕ сериализует api_key; поле читается только из env. Repr Settings не печатается в коде плана. |
| T-05-03 | Information Disclosure | document_id/model_version провенанс | accept | Неконфиденциальные значения (hash + id модели); безопасны для логов/графа. |
</threat_model>

<verification>
- `pytest tests/test_extractor_schema.py` зелёный.
- `python -c "from core.extractor import ExtractedCandidate; print(ExtractedCandidate.model_fields.keys())"` показывает full_name, contacts, experiences, education, skills, document_id, model_version.
- `python -c "from core.config import get_settings; print(get_settings().extractor_model)"` печатает qwen/qwen3.6-plus.
- `ruff check core/extractor core/config.py tests/test_extractor_schema.py` чисто.
- `mypy core/extractor` чисто.
</verification>

<success_criteria>
- ExtractedCandidate покрывает EXTR-03 (full_name, contacts, experiences с датами/компанией/ролью/навыками, education, skills) + провенанс.
- Experience.is_current вычисляется (D-05); поля не переименованы под онтологию (D-06).
- Все 5 эталонных parsed.json валидируются (фундамент критерия #4).
- Settings несёт конфиг-кнобы экстрактора.
</success_criteria>

<output>
After completion, create `.planning/phases/05-llm/05-01-SUMMARY.md`
</output>
